package ZadaniaRekrutacyjne.Tablice;

public class ZRTablice1 {

    public static void main(String[] args) {

        int[] numbers = {1,2,3,4,5};

//    z1. zamienić pierwszy element tablicy z ostatnim
//        int first = numbers[0];
//        int last = numbers[numbers.length - 1];
//
//        numbers[0] = last;
//        numbers[4] = first;
////        numbers[numbers.length - 1] = first;
//
//        for (int number : numbers) {
//            System.out.println(number);
//        }

        //    z1a. zamienić pierwszy element tablicy z ostatnim, z minimalną ilością dodatkowych zmiennych
//        int first = numbers[0];
//        numbers[0] = numbers[numbers.length - 1];
//        numbers[numbers.length -1] = first;
//
//        for (int number : numbers) {
//            System.out.println(number);
//        }

//        //    z1b. zamienić pierwszy element tablicy z ostatnim, żadnych dodatkowych zmiennych
//        numbers[numbers.length -1] = numbers[numbers.length -1] + numbers[0]; //suma ostatniego i pierwszego elementum do ostatniego dajemy sume
//        numbers[0] = numbers[numbers.length -1] - numbers[0]; //odejmujemy od sumy 1 i mamy 5 na początku
//        numbers[numbers.length -1] = numbers[numbers.length -1] - numbers[0]; //odejmujemy od szóstki jeden i 5 na końcu mamy 1
//        for (int number : numbers) {
//            System.out.println(number);
//        }


//        1. policzyć ile jest liczb parzystych a ile jest nie parzystych

    }
}
